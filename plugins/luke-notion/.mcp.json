{
  "mcpServers": {
    "luke-notion": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/mcp-server/notion-dump.mjs"],
      "env": {
        "NOTION_TOKEN": "${NOTION_TOKEN}"
      }
    }
  }
}
